# Windows のバッチファイルは CRLF 改行を保つ（cmd.exe での誤動作を防ぐ）
*.bat text eol=crlf

# Word マクロは日本語版 Word の VBA エディタに合わせて Shift-JIS(CP932)・CRLF で保存。
# git で文字コード／改行を変換させないよう、バイナリ扱いにする
# （UTF-8 にすると VBA 取り込み時に日本語コメントが文字化けするため）
*.bas binary

# PDF はバイナリ（改行変換などをしない）
*.pdf binary
